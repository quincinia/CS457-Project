clang++-7 -pthread -std=c++17 -o main main.cpp -lstdc++fs

Keywords list:

---Project 1---

CREATE DATABASE <db_name>
// initializes a new db/db file
// if a db with that name exists, return an error message:
//    "!Failed to create database <db_name> because it already exists."
// otherwise, create the new db and output:
//    "Database <db_name> created."

CREATE TABLE <name> (<value1> <type>[, <value2>, <type>, [...]])
// new table with that name
// the values list initializes the table with those values AND datatypes
// 

DROP DATABASE <db_name>
// deletes the db
// if successful:
//    "Database <db_name> deleted.
// otherwise:
//    "!Failed to delete <db_name> because it does not exist"


DROP TABLE <table_name>
// same as DROP DATABASE except replace "Database" with "Table"

USE <db_name>
// changes context to work within the current 
// if successful:
//    "Using database <db_name>."
// otherwise:
//    <no error message given>


SELECT <attributes list> FROM <table>
// queries a table depending on given attributes 
// if successful:
//    outputs a list of <value> <type>, delimited by "|"
// otherwise:
//    "!Failed to query table <table> because it does not exist."


ALTER TABLE <name> <action> <value>
// modifies the table based on <action>
// values of <action>:
//    ADD -> adds a new item (not sure if it will support lists)
// if successful:
//    "Table <name> modified."

.EXIT
// ends the program
// output: "All done."

---Project 2---

INSERT INTO <table_name> values(<value1> [, <value2>[, ...]]);
// number of values must match the number of attributes of the table
// otherwise, return an error

UPDATE <table_name> SET <attr_name> = <value1> WHERE <attr_name> = <value2>;
// 

Doxygen:
/**
 * @brief  
 * @param  
 * @pre    
 * @return 
 */
